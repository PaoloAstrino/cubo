sentence-transformers>=4.0.0
torch==2.5.1
torchvision==0.20.1
transformers>=4.30.0
ollama
colorama
python-docx
PyPDF2
pyinstaller
chromadb==0.4.18
tokenizers==0.21.0
numpy==1.26.0
protobuf==3.20.3
pytest  # For unit testing
pytest-cov  # For test coverage
# Excluded heavy/unused packages to reduce build time and conflicts
# tensorflow  # Excluded to avoid CUDA warnings and build slowdown
# sklearn  # Use scikit-learn directly if needed
