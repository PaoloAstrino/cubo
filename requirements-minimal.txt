# Minimal requirements for CUBO RAG (Ollama-only mode)
# Uses Ollama for both LLM and embeddings - no PyTorch needed!
# Install size: ~200MB vs ~3GB for full requirements

# === Web Server ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# === Document Processing ===
python-docx>=1.0.0
PyPDF2>=3.0.0
pdfplumber>=0.10.3
Pillow>=10.0.0
openpyxl>=3.1.0

# === Vector Store ===
faiss-cpu==1.7.4
numpy>=1.21.0,<2.0.0

# === LLM & Embeddings (via Ollama - no torch!) ===
ollama>=0.3.0

# === Clustering (minimal) ===
datasketch
scikit-learn==1.3.2

# === Logging ===
structlog
python-json-logger
colorama>=0.4.0
tqdm>=4.64.0

# === Utilities ===
psutil>=5.9.0
