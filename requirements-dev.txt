# Development dependencies

# Testing
pytest>=7.0.0  # For unit testing
pytest-cov>=4.0.0  # For test coverage
pytest-asyncio>=0.21.0  # For async test fixtures
pytest-mock>=3.10.0  # For mocking in tests
requests>=2.28.0

# E2E Testing (new)
playwright>=1.40.0  # Browser automation for frontend tests
reportlab>=4.0.0    # PDF generation for multimodal tests
psutil>=5.9.0       # Memory monitoring for laptop mode tests

# Code quality and formatting
pre-commit>=3.5.0  # Pre-commit hooks
black>=24.0.0  # Code formatter
ruff>=0.7.0  # Fast Python linter
isort>=5.13.0  # Import sorter
mypy>=1.13.0  # Type checker
bandit[toml]>=1.7.0  # Security scanner

# Build and packaging
pyinstaller  # For building executables

# Optional dependencies
onnxruntime  # Optional: ONNX runtime for embedding functions
PySide6>=6.5.0  # For desktop GUI development
reportlab>=4.0
rouge-score  # For evaluating summaries
nltk  # For NLP tasks
scikit-learn  # For F1-score calculation
hdbscan
umap-learn
pandas
openpyxl
pyarrow
filelock
requests  # For HTTP calls in manual scripts and live tests

# Optional: Faiss (CPU/GPU builds) is not included by default because it is platform-specific.
# Install via pip on Linux/macOS: `pip install faiss-cpu` (or `faiss-gpu` for GPU builds).
# On Windows use conda: `conda install -c conda-forge faiss-cpu`.
